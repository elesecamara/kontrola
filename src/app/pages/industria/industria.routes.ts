import { Routes } from '@angular/router';
import { IndustriaComponent } from './industria.component';

export const INDUSTRIA_ROUTES: Routes = [
  { path: '', component: IndustriaComponent, title: 'Industria | Kontrola' },
];