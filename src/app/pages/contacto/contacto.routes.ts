import { Routes } from '@angular/router';
import { ContactoComponent } from './contacto.component';

export const CONTACTO_ROUTES: Routes = [
  { path: '', component: ContactoComponent, title: 'Contacto | Kontrola' },
];